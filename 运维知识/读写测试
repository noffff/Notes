Fio-flexible I/O tester
能够产生多个线程或进程进行特殊类型的I/O动作。可以定义一个job然后模拟出I/O操作
参数：
	name=str          
	该参数被用来覆盖job名，如果在命令行输入，该参数可以发出一个信号开始一个新的job。
	
	description=str
	没有特殊的其他用途，只是描述该job是做什么的
	
	directory=str
	文件名的前缀。文件放置的位置。可以通过':'符号从而指定多个目录。这些目录都会