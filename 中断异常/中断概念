中断(interrupt)一般被定义为一个事件，事件能够改变处理器执行的指令顺序，该事件与CPU芯片内外部硬件电路产生的电信号相互对应。
interrupt分为同步(synchronous)中断和异步(asynchronous)中断。
同步中断一般叫做“异常”，异步中断叫做“中断”。
同步中断是当只有在指令终止执行后CPU才会发出中断。
异步中断由其他设备随CPU时钟信号随机产生。
异常一般分为两类：
	1.程序错误引起
	2.由内核必须处理的异常产生(如缺页)
对于中断信号的处理与进程间的切换比较类似，但中断处理和进程切换有一个明显差异
